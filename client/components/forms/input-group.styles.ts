import {CSSProperties} from "react";

export const styles = {
    inputGroup: {
        padding: "15px 20px"
    },
    labelName: {
        textTransform: "capitalize",
        marginBottom: "5px",
    } as CSSProperties,
    input: {
        height: "20px",
        padding: "5px 7px",
        borderRadius: "3px",
        border: "1px solid dimgrey"
    } as CSSProperties,
    label: {
        display: "flex",
        flexDirection: "column"
    } as CSSProperties,
}
