import {CSSProperties} from "react";

export const styles = {
    formTitle: {
        margin: "10px 17px"
    },
    form: {
        borderRadius: "3px",
        border: "1px solid dimgrey",
        maxWidth: "300px",
        display: "flex",
        flexDirection: "column",
    } as CSSProperties,
}
